import matplotlib.pyplot as plt

# CREATE the figure No. 10 and GENERATE axes (a space where actually plotting occurs) in the figure, automatically setting 2x2 grid.
fig, ax = plt.subplots(2, 2, num = 10)
''' GENERATED AXES:
ax[0, 0], ax[0, 1], ax[1, 0], ax[1, 1]
'''

plt.show()